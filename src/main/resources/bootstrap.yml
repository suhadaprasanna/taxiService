spring:
    application:
        name: gdc-taxi-service
    profiles:
        active : dev
    cloud:
      config:
        enabled: true
        uri: http://localhost:8888/

# spring.profiles.active : dev